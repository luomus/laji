import { ExcludeSubcategoryPipe } from './exclude-subcategory.pipe';

describe('ExcludeSubcategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new ExcludeSubcategoryPipe();
    expect(pipe).toBeTruthy();
  });
});

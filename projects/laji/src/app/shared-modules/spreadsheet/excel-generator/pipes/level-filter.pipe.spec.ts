import { LevelFilterPipe } from './level-filter.pipe';

describe('LevelFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LevelFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
